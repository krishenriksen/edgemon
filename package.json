{
  "name": "edgemon",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "pnpm run -r build",
    "format": "prettier --write .",
    "lint": "prettier --check . && pnpm run -r lint"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
